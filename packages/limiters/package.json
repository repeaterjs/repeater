{
  "name": "@channel/limiters",
  "version": "0.1.1",
  "description": "Basic async iterators for limiting concurrency, implemented with channels",
  "license": "MIT",
  "homepage": "https://github.com/channeljs/channel",
  "files": [
    "/lib"
  ],
  "main": "lib/index.cjs.js",
  "module": "lib/index.esm.js",
  "types": "lib/index.d.ts",
  "scripts": {
    "prebuild": "yarn run clean",
    "build": "rollup -c ../../rollup.config.js",
    "clean": "shx rm -rf ./lib",
    "test": "jest --config ../../jest.config.js --rootDir --color",
    "lint": "eslint --ext ts src",
    "prepublishOnly": "yarn run test && yarn run build"
  },
  "dependencies": {
    "@channel/channel": "^0.1.1",
    "@channel/timers": "^0.1.1"
  },
  "publishConfig": {
    "access": "public"
  }
}
