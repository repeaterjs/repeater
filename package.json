{
  "name": "@collabjs/channel",
  "version": "0.1.0",
  "private": true,
  "main": "lib/index.js",
  "scripts": {
    "clean": "shx rm -rf ./lib",
    "prebuild": "yarn clean",
    "build": "tsc",
    "watch": "yarn run build -w",
    "prebench": "yarn build",
    "bench": "NODE_ENV=production node ./bench",
    "develop": "yarn test --watchAll",
    "test": "jest --verbose",
    "lint": "eslint --ext js,ts src"
  },
  "devDependencies": {
    "@types/jest": "^24.0.11",
    "@typescript-eslint/eslint-plugin": "^1.5.0",
    "@typescript-eslint/parser": "^1.5.0",
    "eslint": "^5.15.3",
    "eslint-config-prettier": "^4.1.0",
    "eslint-plugin-prettier": "^3.0.1",
    "jest": "^24.5.0",
    "prettier": "^1.16.4",
    "shx": "^0.3.2",
    "ts-jest": "^24.0.0",
    "typescript": "^3.3.4000"
  }
}
