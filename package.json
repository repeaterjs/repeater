{
  "private": true,
  "workspaces": [
    "packages/repeater",
    "packages/timers",
    "packages/limiters",
    "packages/pubsub"
  ],
  "scripts": {
    "build": "yarn workspaces run build",
    "lint": "yarn workspaces run lint",
    "test": "yarn workspaces run test"
  }
}
